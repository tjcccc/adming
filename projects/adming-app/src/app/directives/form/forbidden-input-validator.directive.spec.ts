import { ForbiddenInputValidatorDirective } from './forbidden-input-validator.directive';

describe('ForbiddenInputValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenInputValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
