import { CollapseNextDirective } from './collapse-next.directive';

describe('CollapseNextDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapseNextDirective();
    expect(directive).toBeTruthy();
  });
});
