import { NextRowTriggerDirective } from './next-row-trigger.directive';

describe('NextRowTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new NextRowTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
